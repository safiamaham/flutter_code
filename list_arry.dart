main() {
//   List studentnames = ["safia", "maham", "kainat", "kulsoom", "warisha"];
//   // print(studentnames[studentnames.length - 2]);

//   // studentnames
//   //     .replaceRange(studentnames.length - 1, studentnames.length, ["def"]);
//   // print(studentnames);

//   // studentnames.sort();
//   // print(studentnames);

  //List studentnames = ["safia", "maham", "khadija", "mehreen", "batool"];
  // for sorting
//   var sum = List.of(studentnames);
//  sum.sort();
//   print(sum);

  // for empty and not empty
  // print(studentnames.isEmpty);

  // studentnames.Clear;
  //   print(studentnames.isEmpty);

  //  for adding element
  // studentnames.add("hellow");
  // print(studentnames);
  // studentnames.addAll(["ddd", "abc", "safia"]);
  //studentnames.insert(0, "abc");
  // studentnames.insertAll(2, ["a", "b"]);
  // print(studentnames);

  // for reverse
  //is tarhan list ko agar assign karky reverse karyn to hamayn list of karky karna hta hai
  // var abc=List.of(studentnames.reversed);
  // abc.add("kkk");
  // print(abc);

  //  for removing
  // studentnames.remove("khadija");
  // print(studentnames);
  // studentnames.removeAt(1);
  //   print(studentnames);

//removerwhere with condition
  // List studentnames = [5, 6, 9, 55, 2, 3];
  // // studentnames.removeWhere((e) => e > 5);
  // studentnames.retainWhere((e) => e < 5);
  // print(studentnames);

//sort at descending order
 // List studentnames = [1, 5, 2, 4, 3, 9, 8, 6, 7, 0];
  // studentnames.sort();
  // //  print(studentnames);
  // var abc=List.of(studentnames.reversed);
  //   print(abc);
  
  //yeh list.of karky or .tolist karky dono sy large bracket mai convert hoti hai list ki type
// List studentnames= [1, 2, 3, 4, 5, 6];
// var evenNumbers = List.of(studentnames.where((n)=>n.isEven ));
// print(evenNumbers);  // Output: [2, 4, 6]
//or 
// List<int> numbers = [1, 2, 3, 4, 5, 6];
// var evenNumbers = numbers.where((number) => number.isEven);
// print(evenNumbers.toList());  // Output: [2, 4, 6]


}
